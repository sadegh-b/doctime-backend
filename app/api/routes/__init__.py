"""
DocTime API routes package.

Router registration is handled explicitly in `app.main`.
Do not import route modules here, because doing so can create
circular imports and import-time side effects.
"""
